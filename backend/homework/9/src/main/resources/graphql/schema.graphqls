type Director {
  id: ID!
  name: String!
  totalAwards: Int
}

type Movie {
    id: ID!
    title: String!
    genre: String!
    comment: String!
    rating: Int!
}

type Query {
    findMovieById(id: ID!): Movie
}

type Mutation {
    addReview(id: ID!, comment: String!, rating: Int!): Movie
}